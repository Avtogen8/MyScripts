Процедура ЗаписатьИнформациюВФайл()

	ЗапТекста = Новый ЗаписьТекста();
	ЗапТекста.Открыть("D:\Antonio\Development\1C\GIT\MyScripts\file.log");

	СИ = Новый СистемнаяИнформация();
	Для Каждого Переменная Из СИ.ПеременныеСреды() Цикл
		ЗапТекста.ЗаписатьСтроку(Переменная.Ключ + " = " + Переменная.Значение);
	КонецЦикла;

КонецПроцедуры

//точка входа
ЗаписатьИнформациюВФайл();